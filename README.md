### Websocket Game monorepo

---
project uses `lerna` so commands in root `websocketgame` folder will let you manage all packages

---
1. run `yarn` in order to install all dependencies and build `dist` folders

2. run `yarn start` to start Client and Server